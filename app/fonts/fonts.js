import { Poppins, Figtree } from 'next/font/google';

export const figtree = Figtree({ subsets: ['latin'], weight: "300" });
export const poppinsBold = Poppins({ subsets: ['latin'], weight: "400" });
export const figtreeBold = Figtree({ subsets: ['latin'], weight: "500" });